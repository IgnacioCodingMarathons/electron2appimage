electron2appimage is a tool where you turn a Linux Electron app into a Linux AppImage! This handles the whole bundling process for you!

This tool is also available through Ubuntu's apt.

```bash
sudo add-apt-repository ppa:coathangergmap/metalppa
sudo apt update
sudo apt install electron2appimage
```
